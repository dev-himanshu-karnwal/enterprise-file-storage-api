# efs client

React + Vite UI for the Enterprise File Storage API.

## Setup

```bash
cd client
npm install
npm run dev
```

App: http://localhost:5173  
API calls go to `/api/*` and are proxied to `http://localhost:8000`.

Optional: set `VITE_API_URL` to hit the API directly (remember CORS).
